# Core pipeline framework
dlt[snowflake,mongodb]>=0.5.0

# Data download & parsing
requests>=2.31
pyarrow>=14.0        # read Parquet files

# MongoDB client (used by dlt MongoDB destination/source)
pymongo>=4.6

# Optional: local validation
pandas>=2.0
